function [ a ] = imageinformation(k)
imshow(k);
%impixel is used for obtainin te pixel value of points marked on image by cursur
a = impixel;
%improfile displays a plot of the data along the line.
imshow(k);
improfile;
end

